name: worker

on:
  push:

jobs:
  goal:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        generation: ["concept", "rapid", "dependent", "oppose", "whip"]
        goods: ["shortcoming", "coal", "square", "giant"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar Vt
